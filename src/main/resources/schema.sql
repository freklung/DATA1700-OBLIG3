CREATE TABLE Billetter
(
    id int NOT NULL AUTO_INCREMENT,
    fValg varchar(255),
    antall int,
    fNavn varchar(255),
    eNavn varchar(255),
    tlfNr varchar(255),
    epost varchar(255),
    PRIMARY KEY (id)
);
